# Python floor for the repo's one Python file (llamacpp/mcp/web-search-mcp.py),
# so ruff and editors lint against the right version. Matches that script's
# PEP 723 requires-python; its runtime deps also live there, not here.

[project]
name = "ollama-modelfiles-tools"
version = "0.1.0"
description = "Tooling config for the ollama-modelfiles repo."
requires-python = ">=3.11"
dependencies = []
