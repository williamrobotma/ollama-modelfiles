# Task Plan: Migrate off Ollama-managed GGUFs -> local HF-cache files

Branch: `migrate/hf-local-ggufs` (off `refactor/benchmark-common-harness`).
Predecessor plan (9B-coders benchmark) completed; see git history and
session_summary_qwen3.5_9b_mtp_bench.md.

## Objective
Every Modelfile references GGUFs provisioned by `hf download` (HF cache,
llama.cpp-convention files) instead of `hf.co/...` OCI pulls or
registry.ollama.ai tags. Rationale: the same files then serve both Ollama
(`FROM /abs/path.gguf`) and llama.cpp directly (`--model/--model-draft`),
the OCI config-blob hang class of failure disappears, and Gemma MTP
(target + separate drafter; llama.cpp-only on CUDA) becomes runnable
from the same cache.

## Scope (FROM-reference classes)
| Class | Files | Action |
|---|---|---|
| Already local | gemma4-26b-a4b-it-qat | done (main + mmproj-BF16, vision verified) |
| hf.co Unsloth | gemma4-12b-it-qat; qwen3.5-9b-mtp-coding-ud-q4-k-xl; qwen3.6-27b-coding-ud-q4-k-xl; qwen3.6-27b-mtp-coding-ud-q4-k-xl; qwen3.6-35b-a3b-coding-ud-q4-k-xl; qwen3.6-35b-a3b-mtp-coding-ud-q4-k-xl; (untracked) qwen3.6-35b-a3b-mtp-coding-ud-q5-k-xl | hf download -> local FROM |
| hf.co community | gemma4-12b-it-obliterated (OBLITERATUS); qwen3.6-27b-obliterated-q4-k-m + -coding (OBLITERATUS, same repo); gemma4-26b-a4b-it-heretic-q4-k-m (mradermacher); qwen3.6-35b-a3b-uncensored-hauhaucs-aggressive-iq4-xs (HauhauCS); qwopus3.5-9b-coder-q4-k-m (Jackrong) | hf download -> local FROM |
| registry.ollama.ai | qwen3.5-9b-coding-q4-k-m (qwen3.5:9b official); qwen3.6-35b-a3b-abliterated-q4-k + claude4.7 (huihui_ai) | needs source research: qwen3.5 -> unsloth/Qwen3.5-9B-GGUF direct download (OCI-bridge memory does NOT cover plain hf download); huihui -> find HF-hosted equivalent, else keep + document exception |
| Thin aliases (6) | *-coding / *-mtp-coding / obliterated unsuffixed | no change (FROM canonical local name) |
| MTP staging (untracked, do-not-delete) | gemma4-26b-a4b-it-qat-mtp | fix to 26B refs + validated llama-server script (Ollama DRAFT is MLX-only; llama.cpp is the CUDA path) |

## Phases / status
- [x] Branch + this plan doc
- [x] Baselines captured: `ollama show` + `--modelfile` for all 38 installed
      models -> scratchpad/baselines (validation reference)
- [ ] llama.cpp CUDA rebuild at today's master (background; RTX 4070 arch 89)
- [ ] Live research (today's state): #24795 MTP-drafter regression fix status;
      ollama release news; per-repo GGUF file+sha256 inventory; qwen3.5-9b and
      huihui source decisions
- [ ] Blob pre-seed validation: ollama blob sha256 == HF LFS oid -> hardlink
      into HF cache (same filesystem, dev 2096) to avoid ~150 GB re-download;
      validate on 26B (known dcf179a9...) then apply broadly
- [ ] Acquire all target GGUFs (`hf download`, background, monitored)
- [ ] Rewrite Modelfiles -> local snapshot paths (+ second FROM mmproj where
      baseline shows a Projector); `ollama create`; validate each against
      baseline (capabilities/params/template); smoke-run subset
- [ ] Gemma 26B MTP end-to-end on llama.cpp/CUDA (fallback: b9553 if master
      still broken); acceptance rate + t/s vs control; fix -mtp Modelfile
- [ ] Retire redundant ollama-managed manifests (after validation; skip loaded)
- [ ] Docs sync (CLAUDE.md conventions, README, new session summary), tidy,
      commit, push

## Constraints / found state
- Disk: 590 GB free; ollama store 256 GB; HF cache 37 GB. Preseed-by-hardlink
  avoids doubling storage for the ~150 GB overlap.
- PROD FINDING: `systemctl show ollama` Environment has NO
  GGML_CUDA_DISABLE_GRAPHS=1 -> prod is still graphs-ON, exposed to the
  ~12.5%/run MTP crash (session_summary_mtp_graphs_crash.md action item was
  never applied; needs sudo, user-only).
- Ollama Modelfile `DRAFT` is MLX+safetensors only (PR #15980); on CUDA it is
  a silent no-op via /api (proven: built -mtp model has no drafter layer).
- llama.cpp #24795: gemma4-assistant drafter load broken b9702+..>=b9672
  ("Gemma4Assistant requires ctx_other"); works b9553. Verify on today's
  master first.
- Claude-Code template gate applies to any re-sourced community GGUF: vet
  `ollama show --template` / GGUF chat_template for multi-system tolerance.

## Resume point
If interrupted: `git log` on migrate/hf-local-ggufs + this file's checkboxes;
baselines in the session scratchpad (regenerate with `ollama show` loop if
lost); research findings land in session_summary_migration_local_ggufs.md.
